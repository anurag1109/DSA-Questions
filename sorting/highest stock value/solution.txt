function stockValue(n,arr){
    // write code here
    let high=0;
    let sum=0;
    for(let i=0;i<n;i++){
        sum+=arr[i];
        if(sum>high)
        high=sum;
        
    }
    console.log(high)
}
