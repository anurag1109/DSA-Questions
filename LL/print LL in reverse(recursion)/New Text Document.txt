const LinkedListNode = class {
    constructor(nodeData) {
        this.data = nodeData;
        this.next = null;
    }
}

// Complete the function below

function reverse(head) {
    if(head.next==null)
    return head
   let x= reverse(head.next)
   head.next.next=head
   head.next=null
   return x
}